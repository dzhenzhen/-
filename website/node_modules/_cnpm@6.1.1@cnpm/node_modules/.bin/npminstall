#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_npminstall@3.27.0@npminstall/bin/install.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_npminstall@3.27.0@npminstall/bin/install.js" "$@"
  ret=$?
fi
exit $ret
